import React from "react";

import { Wrap } from "./Wrap";

export default {
  title: "Warp",
  component: Wrap,
};

const Template = (args) => <Wrap {...args} />;

export const Default = Template.bind({});
